export default class Usuario {
    constructor(nom, apel, mail, pass, esAdmin) {
        this.nombre = nom;
        this.apellido = apel;
        this.email = mail;
        this.password = pass;
        this.esAdmin = esAdmin;
    }
}
